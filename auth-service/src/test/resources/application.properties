## Test-only properties: prevent production FirebaseConfig from running during unit tests
# Provide safe defaults for firebase properties used in some tests
app.firebase.credentials-path=
app.firebase.project-id=test-project
spring.main.allow-bean-definition-overriding=true
